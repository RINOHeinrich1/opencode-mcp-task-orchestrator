# Rapport MERGE/PUSH — T-20260921-140612-t8ub

- **Tâche** : `T-20260921-140612-t8ub` — Panneau — performance : chargement Fonctionnalités/Règles métier (N+1 de 55 requêtes + rejeu du schéma à chaque appel MCP)
- **Exécution** : `E-T-20260921-140612-t8ub-vklw72`
- **Plan** : `Plan-panneau-perf-fr-nplus1-mcp-20260921-141337`
- **Projet** : `ecosystem` (org `onirtech`)
- **Date** : 2026-09-21 14:32 UTC
- **Review** : APPROUVÉE par l'humain (préalable à cette étape)

## 1. Résumé

Étape MERGE/PUSH de la tâche : pour les **2 repos**, la branche de travail a été mergée
(fast-forward) dans la branche de déploiement `feature/migration-postgresql`, synchronisée
sur `origin` puis poussée. Les vérifications post-merge ont été exécutées **avant** le push
(6 fichiers `node --check` OK + contrôles fonctionnels ciblés). Les 2 worktrees et les 2
branches de travail ont été nettoyés. **Aucun redémarrage PM2** (pris en charge par
l'orchestrateur). Les checkouts principaux sont restés sur `feature/migration-postgresql`.

## 2. Isolation

- **Espace Coder** : non applicable — outillage écosystème local (repos `/root/...`),
  conformément aux chemins de scope de la tâche.
- **Session guard** : `acquire` en mode **`in-place`** sur les 2 repos (aucune session
  parallèle détectée) :
  - `/root/.config/opencode/mcp/task-orchestrator` → `mode: in-place`, branche `feature/migration-postgresql`
  - `/root/orchestrator-panel` → `mode: in-place`, branche `feature/migration-postgresql`
- **Session opencode** : `ses_f3b9eb998ffeZAcAcs5R7nWU6o`
- Pas de worktree créé par cette session (travail dans les checkouts principaux, mode in-place).
- Verrous session-guard **libérés** en fin de traitement.

## 3. Branches et commits

### Repo 1 — `opencode-mcp-task-orchestrator` (`/root/.config/opencode/mcp/task-orchestrator`)
- Branche de travail : `build-notify/mcp-perf-fr-nplus1` (commit `b77cbeb`, base `e576bb7`)
- Branche de déploiement : `feature/migration-postgresql` → `feature/migration-postgresql`
- **Merge** : fast-forward `e576bb7..b77cbeb` → **sha de merge `b77cbeb`**
- **Push** : `e576bb7..b77cbeb  feature/migration-postgresql -> feature/migration-postgresql` (OK)
- Commit : `b77cbeb perf(mcp): skip rejeu schéma via schema_meta + compteurs links bulk sur feature_list/rule_list (A002-A009) (T-20260921-140612-t8ub)`
  - fichiers : `db.mjs`, `index.mjs`, `schema.sql`

### Repo 2 — `opencode-observability` (`/root/orchestrator-panel`)
- Branche de travail : `build-notify/panel-perf-fr-nplus1` (commit `4fd70a0`, base `281acc9`)
- Branche de déploiement : `feature/migration-postgresql` → `feature/migration-postgresql`
- **Merge** : fast-forward `281acc9..4fd70a0` → **sha de merge `4fd70a0`**
- **Push** : `281acc9..4fd70a0  feature/migration-postgresql -> feature/migration-postgresql` (OK)
- Commit : `4fd70a0 perf(panneau): onglet Fonctionnalités/Règles sans N+1 (index dérivé de links) + client MCP persistant (A001,A010-A013) (T-20260921-140612-t8ub)`
  - fichiers : `mcp-client.mjs`, `public/app.js`, `scripts/bench-fr-load.mjs`, `server.mjs`

Synchronisation préalable : `git fetch origin --prune` + `git pull --rebase origin feature/migration-postgresql`
sur les 2 repos (« Already up to date ») avant merge. **Aucun force-push.**

## 4. Vérifications post-merge

### Repo MCP (`task-orchestrator`)
| Contrôle | Résultat |
|---|---|
| `schema_meta` (table) | OK — `schema.sql:11` `CREATE TABLE IF NOT EXISTS schema_meta (key, value, updated_at)` |
| `SCHEMA_VERSION` | OK — `db.mjs:33` `const SCHEMA_VERSION = "2026-09-21-perf-fr-nplus1"` |
| `ensureSchema` skip | OK — `db.mjs:65-95` : chemin rapide si `readSchemaVersion() === SCHEMA_VERSION` (aucun DDL) ; sinon apply complet sous `pg_advisory_lock(SCHEMA_LOCK_KEY)` + re-check + `writeSchemaVersion` ; `_schemaPromise.catch` → retentative |
| `featureLinkCounts` / `ruleLinkCounts` | OK — `db.mjs:2013` / `db.mjs:2043` (1 requête bulk `unnest($1::text[])` + sous-requêtes count) |
| `links` dans `listFeatures` / `listRules` | OK — `db.mjs:2084` (features) et `db.mjs:2251` (rules) : `links` ajouté au retour, zéros par défaut |
| `node --check` | OK — `db.mjs`, `index.mjs` |

### Repo panneau (`opencode-observability`)
| Contrôle | Résultat |
|---|---|
| `buildFeatureRuleLinkIndex` (app.js) | OK — `public/app.js:5192` (définition, synchrone) + `:5546` (usage, remplace l'ancien `loadFeatureRuleLinkIndex` N+1) |
| Clients MCP persistants (mcp-client.mjs) | OK — `mcp-client.mjs:51` `const clients = new Map()` (clé `serveur::lane`), spawn paresseux + réutilisation, lanes `main`/`long`, respawn si mort |
| `closeAllMcpClients` (mcp-client.mjs) | OK — `mcp-client.mjs:168` (kill de tous les process persistants) + filet `process.on("exit")` |
| Handlers SIGTERM/SIGINT (server.mjs) | OK — `server.mjs:2928` (SIGTERM) / `:2929` (SIGINT) → `shutdown()` → `closeAllMcpClients()` |
| `node --check` | OK — `mcp-client.mjs`, `public/app.js`, `server.mjs`, `scripts/bench-fr-load.mjs` |

**Bilan** : 6 fichiers JS `node --check` **OK** ; tous les points de contrôle présents et conformes.

## 5. État final des checkouts principaux

| Repo | Branche | HEAD | vs origin | Working tree |
|---|---|---|---|---|
| `/root/.config/opencode/mcp/task-orchestrator` | `feature/migration-postgresql` | `b77cbeb` | à jour (`...origin/feature/migration-postgresql`, rien à pousser) | tracked propre (fichiers `plans/`/`reports/` non suivis — artefacts d'orchestration, comportement inchangé) |
| `/root/orchestrator-panel` | `feature/migration-postgresql` | `4fd70a0` | à jour (`...origin/feature/migration-postgresql`) | **propre** |

Les deux checkouts principaux sont bien restés sur `feature/migration-postgresql`
(pas sur une branche de travail) — le panneau live sert donc les statiques du working
tree dans leur version déployée.

## 6. Nettoyages

- Worktree `/root/.config/opencode/mcp/task-orchestrator-wt-mcp-perf-fr-nplus1` → **supprimé**
- Branche `build-notify/mcp-perf-fr-nplus1` (was `b77cbeb`) → **supprimée** (`git branch -d`, mergée)
- Worktree `/root/orchestrator-panel-wt-panel-perf-fr-nplus1` → **supprimé**
- Branche `build-notify/panel-perf-fr-nplus1` (was `4fd70a0`) → **supprimée** (`git branch -d`, mergée)
- Worktrees/branches des **autres** tâches en cours : **non touchés** (feature-rule-crud-liaisons,
  pieces-client-projet, sprint-crud-mcp, sprint-cycle-de-vie-rapport).
- **PM2 : aucun redémarrage** effectué (à la charge de l'orchestrateur).

## 7. Avertissements / erreurs

- Aucun conflit, aucun force-push, aucun blocage.
- Le checkout principal MCP contient des fichiers non suivis (`plans/*.md`, `reports/*.md`) :
  artefacts d'orchestration attendus, sans incidence sur le merge ni le build.
- Le remote git embarque des identifiants dans l'URL (`https://<user>:<token>@github.com/...`) ;
  ils ont été **masqués** dans ce rapport et ne sont jamais reproduits.

## 8. Prochaines étapes / recommandations

1. **Orchestrateur** : redémarrer le panneau PM2 pour charger `mcp-client.mjs` (clients persistants)
   et `server.mjs` (handlers SIGTERM/SIGINT) — non fait ici volontairement.
2. Vérifier au prochain démarrage que le marqueur `schema_meta.schema_version = '2026-09-21-perf-fr-nplus1'`
   est bien écrit après le premier apply (le 1er process paie le DDL, les suivants sautent).
3. Recette : valider le chargement de l'onglet Fonctionnalités/Règles sur `myxmax` (45+10) < 1 s
   (mesure de référence : `reports/report-perf-fr-load-20260921-142952.md`).
